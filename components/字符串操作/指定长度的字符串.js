/*
 * author ganxianjun
 * tool 指定长度的字符串
 * lenS 最小长度 lenM 最大长度
 * lenStringFn 函数名
 * str 数字，字母，下划线组成
*/
function lenStringFn(s, m) {
    // 生成 x >= s && x <= m （ math.random => [0, 1) ）
    // strLen = x
    // 遍历x遍，生成x个字符，返回

    //  1- 10
    math.floor(math.random * 10 + 1);

}
